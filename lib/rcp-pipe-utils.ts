export const RCP_PIPE_JOINT_TYPE = "O-Ring";

export function normalizeRcpPipeJointType(
  _value: string | null | undefined,
): string {
  return RCP_PIPE_JOINT_TYPE;
}

export function formatRcpPipeJointTypeLabel(): string {
  return RCP_PIPE_JOINT_TYPE;
}
